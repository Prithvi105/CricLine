import { styled } from "styled-components";

const Wrapper = styled.section`

    .inner{
        display : flex;
        flex-wrap : wrap;
        input{
            flex : 1;
            min-width : 100px;
            min-height : 100px;
            font-size : 1.2rem;
            cursor : pointer;
        }
    }

`

export default Wrapper